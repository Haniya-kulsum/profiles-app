# Profiles App

Profiles App is a simple React application built with Vite that showcases the use of React components and Bootstrap styling. It currently displays a "Hello React" message using React-Bootstrap, demonstrating how to integrate UI libraries into a React project for clean and responsive design.
